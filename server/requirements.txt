# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# --- Settings / validation ---
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# --- Database (PostgreSQL, async) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0

# --- Auth ---
pyjwt==2.10.1
bcrypt==4.2.1
google-auth==2.36.0
requests==2.32.3  # transport used by google.auth.transport.requests for verify_oauth2_token

# --- AI ---
anthropic==0.40.0
google-genai==0.3.0   # Gemini — image analysis + report writing — AI_PROVIDER=gemini

# --- Billing ---
stripe==11.3.0

# --- Object storage ---
boto3==1.35.79           # S3 / S3-compatible — STORAGE_PROVIDER=s3|s3_compatible
firebase-admin==6.6.0    # Firebase Storage — STORAGE_PROVIDER=firebase

# --- PDF generation ---
reportlab==4.2.5

# --- Utilities ---
python-dotenv==1.0.1
httpx==0.27.2
anyio==4.6.2.post1
